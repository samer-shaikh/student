name: first

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - uses: iterative/setup-cml@v2
          
      
      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-


      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --no-cache-dir --prefer-binary -r requirements.txt 

      - uses: keithweaver/aws-s3-github-action@v1.0.0
        with:
          command: cp
          source: ./Exam_Score_Prediction.csv
          destination: s3://students-mlops/Exam_Score_Prediction.csv
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1

      - name: Run DVC pipeline
        run: dvc repro

      - name: Push data & models
        run: dvc push

      - name: Create ML report
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "# 📊 Model Evaluation Report" > report.md
          echo "## Metrics" >> report.md
          cml metrics dvclive/plots/metrics/*.tsv >> report.md
          cml comment create report.md
